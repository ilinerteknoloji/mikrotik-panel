import type { Metadata } from "next";

type Props = {};

export const metadata: Metadata = {
  // TODO: Add metadata DevicesPage
};

export default async function DevicesPage({}: Props) {
  return <>Devices Page</>;
}
